# Auto detect text files and perform LF normalization
* text=auto

# Shell scripts must always use LF, even on Windows checkouts — they run on
# the Linux VPS and a CRLF shebang line breaks them immediately.
*.sh text eol=lf
